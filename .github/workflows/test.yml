name: Test FastAPI Project

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Hello CI/CD
        run: |
          echo "===== Hello I am testing CI/CD====="

      - name: Show Docker images
        run: docker images